enum UserType { driver, passenger }

enum UserStatus { active, inactive, suspended, banned }

enum RideStatus { requested, accepted, rejected, inProgress, completed }

enum PaymentStatus { pending, completed, failed }

enum PaymentType { cash, card }

enum VehicleType { car, bike, auto }
